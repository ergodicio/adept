name: adept
channels:
  - conda-forge
dependencies:
  - python=3.10
  - ipython
  - ipykernel
  - pip
  - pip:
      - "jax[cpu]"
      - numpy
      - scipy
      - matplotlib
      - pyyaml
      - mlflow
      - boto3
      - flatdict
      - tqdm
      - xarray
      - diffrax
      - h5netcdf
      - optax
      - jaxopt
      - pint